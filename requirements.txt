# Install numpy first (required by other packages)
numpy>=1.18.0

# Core dependencies
Django>=3.2,<4.0
setuptools
wheel

# Scientific packages (need numpy)
scikit-learn==0.22.1
pandas>=0.25.3
matplotlib>=3.1.3

# Audio processing (need numpy)
numba>=0.57.0
soundfile
librosa>=0.10.0
joblib

# Production server
gunicorn>=20.1.0
whitenoise>=6.0.0
psycopg2-binary
