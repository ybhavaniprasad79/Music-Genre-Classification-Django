# Install numpy first (required by other packages)
# Pin to <1.24 for compatibility with old scikit-learn 0.22.1 (uses deprecated np.float)
numpy>=1.18.0,<1.24

# Core dependencies
Django>=3.2,<4.0
setuptools
wheel

# Scientific packages (need numpy)
scikit-learn==0.22.1
scipy>=1.5,<1.11
pandas>=0.25.3,<2.2
matplotlib>=3.1.3,<3.8

# Audio processing (need numpy)
numba>=0.57.0,<0.61
soundfile
librosa>=0.10.0
joblib

# Production server
gunicorn>=20.1.0
whitenoise>=6.0.0
psycopg2-binary
